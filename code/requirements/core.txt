# Core dependencies - Required for the pipeline to run
# These are needed regardless of which components you use

# Progress bars for pipeline execution
tqdm>=4.65.0

# Environment variable management (if using .env files)
python-dotenv>=1.0.0

# Configuration file support (JSON/YAML)
pyyaml>=6.0

